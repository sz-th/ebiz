FROM golang:1.26-alpine AS build
WORKDIR /src
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 go build -o /out/ebiz-server .

FROM alpine:3.20
WORKDIR /app
COPY --from=build /out/ebiz-server /app/ebiz-server
EXPOSE 8080
CMD ["/app/ebiz-server"]
